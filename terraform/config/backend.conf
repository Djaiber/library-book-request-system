# bucket name is called from .env file via ENV_TF_STATE_BUCKET variable
key            = "environments/terraform.tfstate"
region         = "us-east-2"
# S3-native locking (use_lockfile=true) is handled by Terraform — no extra AWS resource needed (dynamodb_table = "terraform-state-lock").
use_lockfile   = true
encrypt        = true